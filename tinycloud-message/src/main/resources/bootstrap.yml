# 项目相关配置
tinycloud:
  # 名称
  name: tinycloud-message
  # 版本
  version: 1.0.0

# Tomcat
server:
  port: 8083
  servlet:
    context-path: /message-server

# Spring
spring:
  application:
    # 应用名称
    name: tinycloud-message
  profiles:
    # 环境配置
    active: dev