# 项目相关配置
tinycloud:
  # 名称
  name: tinycloud-ability
  # 版本
  version: 1.0.0

# Tomcat
server:
  port: 8082
  servlet:
    context-path: /ability-server

# Spring
spring:
  application:
    # 应用名称
    name: tinycloud-ability
  profiles:
    # 环境配置
    active: dev

