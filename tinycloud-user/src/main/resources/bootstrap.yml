# 项目相关配置
tinycloud:
  # 名称
  name: tinycloud-user
  # 版本
  version: 1.0.0
  # 是否开启swagger
  swagger-enable: true

# Tomcat
server:
  port: 8081
  servlet:
    context-path: /user-server

spring:
  profiles:
    active: dev
  application:
    # 应用名称
    name: tinycloud-user